# Make routes into an SF object

Make similar to \`gtfs_trips_sf\` expect one trip is chosen to represent
each route

## Usage

``` r
gtfs_routes_sf(gtfs)
```

## Arguments

- gtfs:

  a gtfs object

## Value

sf data frame of lines
